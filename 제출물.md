# 디지털혁신 사업계획 — 사업 상세문서 분야별 자동분류 결과

## 1. 과제 개요

`사업상세/` 폴더의 사업 상세문서(`.md`) 35건을 각 문서에 기재된 `분야:` 값에 따라 자동 분류하고, 분야별 건수를 집계하여 CSV로 저장하였음.

## 2. 산출물

| 산출물 | 설명 |
| --- | --- |
| `solution.py` | 분야별 자동분류 스크립트 (표준 라이브러리만 사용) |
| `결과_분류현황.csv` | 분야별 건수 집계 (헤더 `분야명,건수` / 건수 내림차순 / UTF-8) |
| `제출물.md` | 본 요약 보고서 |
| `출력_분야별/` | 분야별 하위 폴더에 분류 완료된 사본 |

## 3. 처리 절차

1. **입력** — `사업상세/*.md` 파일을 파일명 순으로 읽음
2. **분야 추출** — 각 문서에서 `분야:` 값을 추출 (`<!-- 분야: X -->` 주석형, `- 분야: X` 목록형 모두 인식)
3. **분류** — `출력_분야별/<분야명>/` 하위로 복사. `shutil.copy2`를 사용하여 **원본은 이동·변경 없이 그대로 보존**
4. **집계** — 분야별 건수를 산출하여 `결과_분류현황.csv` 저장 (건수 내림차순, 동률 시 분야명 가나다순)

## 4. 분류 결과

| 순위 | 분야명 | 건수 | 비율 |
| --- | --- | --- | --- |
| 1 | 디지털전환 | 12건 | 34.3% |
| 2 | 기타 | 8건 | 22.9% |
| 3 | 정보보안 | 8건 | 22.9% |
| 4 | 데이터 | 7건 | 20.0% |
| — | **합계** | **35건** | **100.0%** |

- 총 35건, 4개 분야로 분류 완료
- 최다 분야: **디지털전환** (12건)
- 분야값 누락(미분류) 건수: 0건

## 5. 실행 방법

```bash
python solution.py --clean
```

| 옵션 | 설명 |
| --- | --- |
| `--input` / `-i` | 입력 폴더 지정 |
| `--output` / `-o` | 출력 폴더 지정 |
| `--csv` / `-c` | 결과 CSV 경로 지정 |
| `--md` / `-m` | 요약 보고서 경로 지정 |
| `--clean` | 실행 전 출력 폴더 초기화 |
| `--bom` | CSV를 UTF-8 BOM으로 저장(엑셀 한글 깨짐 방지) |

## 6. 예외 처리

- `분야:` 값을 찾지 못한 파일은 `미분류` 폴더로 분류하고 콘솔에 경고 출력
- 파일명이 중복되면 `이름 (2).md` 형식으로 번호를 부여하여 덮어쓰기 방지
- 분야명에 폴더명으로 쓸 수 없는 문자가 있으면 `_`로 치환
- 입력 인코딩은 UTF-8 → UTF-8(BOM) → CP949 순으로 시도
- 실행 시각 등 비결정적 값을 넣지 않으므로 **같은 입력이면 항상 같은 산출물**이 생성됨
