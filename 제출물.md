# 제출물 — 사업 상세문서 분야별 자동분류

## 사용 AI 도구

- Claude Code (Anthropic Claude Opus 5) — 코드 작성 및 검증

## 구현 메모

- 각 `.md` 문서의 `분야:` 값을 정규식 2종(`<!-- 분야: X -->` 주석형, `- 분야: X` 목록형)으로 추출하고, 값이 없으면 `미분류`로 처리함(현재 0건).
- `shutil.copy2`로 `출력_분야별/<분야명>/`에 복사하여 원본 35건은 이동·변경 없이 보존함.
- 집계는 건수 내림차순, 동률 시 분야명 가나다순으로 정렬해 `결과_분류현황.csv`(UTF-8)에 저장함 — 4개 분야, 최다 디지털전환 12건.
- 표준 라이브러리(argparse·csv·pathlib·re·shutil)만 사용하고, 폴더명 불가 문자 치환·파일명 중복 시 `(2)` 부여·인코딩 3종 폴백을 예외 처리함.
- 실행 시각 등 비결정적 값을 넣지 않아 같은 입력이면 항상 같은 산출물이 생성됨(2회 실행 시 CSV·MD 해시 일치 확인).
